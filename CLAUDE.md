# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a personal Chinese-language programming notes site ("弟弟森的编程小笔记") built with [VuePress 2](https://vuepress.vuejs.org/) and the [vuepress-theme-plume](https://theme-plume.vuejs.press/) theme. Content is Markdown organized by topic; the site deploys to GitHub Pages at the `/short-note/` base path.

## Commands

```sh
pnpm i                  # install (pnpm is required — see packageManager field)
pnpm docs:dev           # dev server on port 9527
pnpm docs:dev-clean     # dev server with cleared cache/temp
pnpm docs:build         # production build (clears cache/temp first)
pnpm docs:preview       # serve the built docs/.vuepress/dist
pnpm vp-update          # update vuepress + theme to latest
```

There is no test suite or standalone lint step. `.markdownlint.json` configures markdownlint for editor integration only.

## Architecture

VuePress config is split across `docs/.vuepress/`:

- `config.ts` — `defineUserConfig`. Holds settings that **restart** the dev server on change: `base`, bundler, `<head>` tags, theme plugin options (Shiki highlighting, markdown features, Giscus comments), and two custom local markdown plugins (see below). `appearance` is `force-dark`; site search is disabled.
- `plume.config.ts` — `defineThemeConfig`. Holds **hot-reloadable** theme settings: logo, profile, footer, navbar, collections, transitions. Do not duplicate keys between this and `config.ts` — `plume.config.ts` wins.
- `navbar.ts` — top navigation. Links point to **permalinks**, not file paths.
- `collections.ts` + `collections/` — one file per topic (e.g. `python.ts`, `vue.ts`), each a `defineCollection`. Re-exported through `collections/index.ts`, aggregated in `collections.ts`. A collection defines the sidebar tree and `autoFrontmatter` for its `dir`.
- `client.ts` — registers custom Vue components (`HomeLanding`, `VPCodeTree`) and the `ArticleMotion` root component; imports `theme/styles/custom.css`.
- `theme/components/` — custom Vue components for the site.

### Permalink-based linking (important)

Every Markdown doc gets a **hashed permalink** in its frontmatter, e.g.:

```yaml
---
title: 容器类型
createTime: 2026/05/27 21:52:13
permalink: /python/umipekr0/
---
```

Navbar and sidebar entries link to these permalinks (`/python/umipekr0/`), **not** to the source file path. Consequences:

- Renaming or moving a `.md` file does not break links as long as its `permalink` stays the same.
- When adding a new doc, its `permalink` is auto-generated by the collection's `autoFrontmatter` on first dev/build. To link it in the navbar or sidebar, use that generated permalink.
- Do not hand-edit a permalink that is already referenced elsewhere without updating every reference.

### Content layout

Topic directories live directly under `docs/` (e.g. `docs/python/`, `docs/vue/`, `docs/javascript/`, `docs/blog/`). Directory names and headings are Chinese. The `blog` collection is type `post`; most others are type `doc` with an explicit `sidebar` tree.

### Custom markdown plugins (in config.ts)

Two `extendsPage`/`extendsMarkdown` plugins run at build time:

- `github-alert-source-class` — tags GitHub-style alerts (`> [!NOTE]`) with a `github-alert-source` CSS class so they can be styled differently from Plume's `::: note` callouts.
- `global-mermaid-init` — auto-injects a `%%{init: ...}%%` directive (hand-drawn look, fixed font/spacing) into Mermaid and related diagram fences that don't already specify one.

## Conventions

- Package manager is **pnpm** (`packageManager: pnpm@11.9.0`); `.npmrc` sets `shamefully-hoist=true` and `pnpm-workspace.yaml` controls hoist/build allowances. Don't switch to npm/yarn.
- Node `^20.6.0 || >=22.0.0` required.
- Content is Chinese-language; match the existing language and tone when editing or adding notes.
- **Quote style**: the author intentionally uses straight ASCII quotes (`"..."` / `'...'`) in place of Chinese quotes (`“...”` / `‘...’`). This is a deliberate personal style — never "correct" straight quotes to Chinese ones.
- `docs/.vuepress/dist`, `.cache`, and `.temp` are build artifacts (gitignored) — never edit them.

## Learning-notes workflow

These notes are written incrementally as the author learns each topic. When collaborating on a note:

- **Reply in Chinese.** All explanations, reviews, and discussion are in Chinese.
- **Generate explanations and code in the chat, not by writing files** — the author copies what they find useful into the note themselves. Only edit `.md` files directly when explicitly asked (e.g. "重排", "帮我改").
- **Structured form preferred.** When explaining a topic, organize it: 概念 → 用法 → 核心特性 → 协作/进阶 → 常见陷阱 → 小结. Use headings, tables, and runnable code blocks.
- **Code must be runnable.** Every Go (etc.) snippet should compile/run as-is so it can be verified with `go run`. Don't post pseudo-code that won't build.
- **Never alter the author's existing wording.** When asked to "重排" / restructure, only move blocks and adjust heading levels — do not rewrite, rephrase, or fix prose. Surface suggested deletions/merges as recommendations for the author to action.
- **Respect topic boundaries.** Keep each note focused on its own subject; a syncing primitive like `WaitGroup` belongs in its dedicated note (`sync 同步原语`), not duplicated into `goroutine`. Distinguish 核心特性 (objective runtime/scheduler behavior) from 常见陷阱 (mistakes from misuse).
- **Verify after edits.** Run `pnpm docs:build` after structural edits to confirm pages still render.

### plume container nesting

When nesting plume containers (`:::details`, `:::note`, `:::table`, `:::code-tabs`, etc.), the **outer** container must use **more** colons than the inner one (markdown-it-container uses colon count to disambiguate boundaries). e.g. outer `:::::details` wraps inner `::::details`. Equal or fewer colons on the outer fence cause the outer block to close prematurely and the nesting to render wrong — `pnpm docs:build` still "succeeds" even when this is broken, so verify the rendered output.
