---
title: 夸标签页通信
createTime: 2025/11/12 21:50:59
permalink: /browser/69atp78u/
---

## BroadCast Channel

`BroadCast Channel` 用于创建一个广播的通信频道。当所有页面都监听同一频道的消息时，其中某一个页面通过它发送的消息就会被所有页面收到，但前提是 ==同源页面==

::: code-tabs
@tab config.js
```js
/**
 * @type {import('vuepress').UserConfig}
 */
const config = {
  // ..
}

export default config
```

@tab config.ts
```ts
import type { UserConfig } from 'vuepress'

const config: UserConfig = {
  // ..
}

export default config
```
:::

## Service Workder

## LocalStorage

## Shared Worker

## IndexDB

## cookie

## window.open 与 window.postMessage

## Websocket
