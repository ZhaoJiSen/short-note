---
title: 字符串
createTime: 2025/11/25 15:34:41
permalink: /rust/nytp8hdg/
---

严格来说，String 不在 std::collections 里，所以通常不把它归为“集合类型”那一组（如 Vec<T>, HashMap<K, V>）。不过从实现上看，String 是基于 Vec<u8> 的可增长、拥有所有权的 UTF-8 字节序列，也就是“存 UTF-8 字节的可变集合”。总结：语义上它是字符串类型；实现上是字节集合；标准库分类上不算在 std::collections 家族。
