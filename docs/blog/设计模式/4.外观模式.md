---
title: 4.外观模式
tags:
  - 设计模式
createTime: 2026/02/27 15:47:31
permalink: /blog/544em2py/
---

外观模式（Facade Pattern）的主要目的是：==给复杂系统加一个 "统一入口"==，让调用方不需要关心内部细节，直接通过外观对象完成业务操作，从而降低调用复杂度和耦合度

:::info 适合使用外观模式的场景

1. 一个业务流程需要串联多个子系统（库存、支付、通知等）
2. 希望对外提供更简单的 API，隐藏内部调用细节
3. 避免调用方直接依赖过多底层模块，减少系统耦合
4. 需要在统一入口加入日志、监控、鉴权、异常处理等横切逻辑

:::

:::code-tabs

@tab 子系统定义

```ts
interface SubmitOrderParams {
    orderId: string;
    skuId: string;
    count: number;
    amount: number;
    userId: string;
}

class InventoryService {
    check(skuId: string, count: number): boolean {
        console.log("检查库存", skuId, count);
        return true;
    }
}

class PaymentService {
    pay(orderId: string, amount: number): boolean {
        console.log("支付成功", orderId, amount);
        return true;
    }
}

class NotifyService {
    send(userId: string, message: string): void {
        console.log("发送通知", userId, message);
    }
}
```

@tab 外观类

```ts
class OrderFacade {
    constructor(
        private inventory = new InventoryService(),
        private payment = new PaymentService(),
        private notify = new NotifyService(),
    ) {}

    submit(params: SubmitOrderParams): void {
        const inStock = this.inventory.check(params.skuId, params.count);
        if (!inStock) {
            throw new Error("库存不足");
        }

        const paid = this.payment.pay(params.orderId, params.amount);
        if (!paid) {
            throw new Error("支付失败");
        }

        this.notify.send(params.userId, "下单成功");
    }
}
```

@tab 使用示例

```ts
new OrderFacade().submit({
    orderId: "O-1001",
    skuId: "SKU-1",
    count: 1,
    amount: 99,
    userId: "U-1",
});
```

:::

在这个案例中把 `inventory.check`、`payment.pay`、`notify.send` 收敛成一步 `orderFacade.submit`
