---
title: 5.组合模式
tags:
  - 设计模式
createTime: 2026/02/27 15:47:30
permalink: /blog/2eppdj25/
---

组合模式（Composite Pattern）的主要目的是把“整体-部分”结构统一成同一套接口，让调用方可以一致地处理单个对象和对象集合

:::info 适合使用组合模式的场景

1. 业务模型天然是树形结构（目录树、菜单树、组织架构）
2. 需要用同一套调用方式处理“叶子节点”和“容器节点”
3. 需要递归执行相同操作（渲染、统计、权限计算等）
4. 希望调用方不频繁判断节点类型，减少条件分支

:::

:::code-tabs

@tab 统一接口

```ts
interface Node {
    render(indent?: number): void;
}
```

@tab 叶子节点与容器节点

```ts
class FileNode implements Node {
    constructor(private name: string) {}

    render(indent = 0): void {
        console.log(`${" ".repeat(indent)}- ${this.name}`);
    }
}

class FolderNode implements Node {
    private children: Node[] = [];

    constructor(private name: string) {}

    add(node: Node): void {
        this.children.push(node);
    }

    render(indent = 0): void {
        console.log(`${" ".repeat(indent)}+ ${this.name}`);
        this.children.forEach((child) => child.render(indent + 2));
    }
}
```

@tab 使用示例

```ts
const root = new FolderNode("src");
root.add(new FileNode("main.ts"));

const components = new FolderNode("components");
components.add(new FileNode("Button.tsx"));
components.add(new FileNode("Card.tsx"));

root.add(components);
root.render();
```

:::
